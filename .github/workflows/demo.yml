name: Demo WorkFlow
on: workflow_dispatch
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print to console step 1
        run: echo Hello step 1 on GH actions
      - name: Print few lines
        run: |
          echo more text line 1
          echo more test line 2
          echo more text line 3
